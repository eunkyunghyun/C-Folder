/* #include <stdio.h>
int get_max(int x, int y);
int get_min(int x, int y);

int main(void)
{
	int a, b;

	printf("두 개의 정수를 입력하세요: ");
	scanf_s("%d %d", &a, & b);

	printf("두 수 중에서 최대값은 %d입니다.\n", get_max(a, b));
	printf("두 수 중에서 최소값은 %d입니다.\n", get_min(a, b));

	return 0;
}

int get_max(int x, int y)
{
	if (x > y) return(x);
	else return(y);
}

int get_min(int x, int y)
{
	if (x < y) return(x);
	else return(y);
}
*/