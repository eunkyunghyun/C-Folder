/* #include <stdio.h>

int main(void)
{
	int i, n;
	int number = 0;

	printf("정수를 입력하세요: ");
	scanf_s("%d", &n);

	for (i = 1; i <= n; i++)
	{
		if (n % i == 0)
			number += 1;
	}

	if (number == 2)
		printf("소수입니다.");
	else
		printf("소수가 아닙니다.");

	return 0;
}
*/