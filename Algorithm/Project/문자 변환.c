/* #include <stdio.h>
#include <ctype.h>

int main(void)
{
	int str;

	while ((str = getchar()) != EOF)
	{
		if (islower(str))
			str = toupper(str);
		putchar(str);
	}

	return 0;
}
*/