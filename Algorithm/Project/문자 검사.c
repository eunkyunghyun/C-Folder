/* #include <stdio.h>
#include <conio.h>
#include <ctype.h>

int main(void)
{
	int str;

	while ((str = _getch()) != 'q')
	{
		printf("====================\n");
		printf("isdigit(%c) = %d\n", str, isdigit(str));
		printf("isalpha(%c) = %d\n", str, isalpha(str));
		printf("islower(%c) = %d\n", str, islower(str));
		printf("ispunct(%c) = %d\n", str, ispunct(str));
		printf("isxdigit(%c) = %d\n", str, isxdigit(str));
		printf("isprint(%c) = %d\n", str, isprint(str));
		printf("====================\n\n");
	}

	return 0;
}
*/