/* #include <stdio.h>

enum days {SUN, MON, TUE, WED, THU, FRI, SAT};
char* days_name[] = { "일요일", "월요일", "화요일", "수요일", "목요일", "금요일", "토요일" };

int main(void)
{
	enum days d;

	d = TUE;

	printf("%s은 %d번째 요일입니다.", days_name[d], d);

	return 0;
}
*/