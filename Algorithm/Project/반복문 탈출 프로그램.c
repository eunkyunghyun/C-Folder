/* #include <stdio.h>
#include <math.h>

int main(void)
{
	double number;

	while (1)
	{
		printf("실수값을 입력하세요: ");
		scanf_s("%lf", &number);

		if (number < 0.0)
			break;

		printf("%f의 제곱근은 ±%f입니다.\n", number, sqrt(number));
		printf("\n");
	}

	return 0;
}
*/